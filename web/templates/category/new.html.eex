<h2>New category</h2>

<%= render "form.html", changeset: @changeset,
                        action: category_path(@conn, :create), items: @items %>

<%= link "Back", to: category_path(@conn, :index) %>
